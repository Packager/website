import * as mixins from "~utils/mixins";
import { media } from "~theme/breakpoints";

export const container = {
  width: "50%"
};

export const iframe = {
  border: "none",
  borderLeft: "1px solid #dcd8d8",
  width: "calc(100% - 1px)",
  height: "100%"
};
