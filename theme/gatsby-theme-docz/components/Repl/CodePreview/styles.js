import * as mixins from "~utils/mixins";
import { media } from "~theme/breakpoints";

export const container = {
  width: "50%"
};

export const iframe = {
  width: "calc(100% - 2px)",
  height: "calc(100% - 2px)"
};
